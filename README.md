# orbit-indexer
Minimal event indexing toolkit for lightweight data pipelines.
## Features - fast event indexing - simple query interface - modular storage - minimal dependencies ## Getting Started ```bash npm install npm run dev
